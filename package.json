{
  "name": "kspdb-fault-detection",
  "version": "1.0.0",
  "description": "Karnataka State Power Distribution Board — Fault Detection & Localization System",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/db/seed.js",
    "test": "node --test server/tests/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
