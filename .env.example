# Server port (default: 3000)
PORT=3000

# Node environment (development | production)
NODE_ENV=development

# OpenAI API key for AI-powered incident summaries (optional)
# If not set, the system falls back to template-based summaries.
OPENAI_API_KEY=

